services:
  - type: web
    name: nexlab-backend
    runtime: node
    plan: free
    buildCommand: |
      apt-get update
      apt-get install -y python3 python3-pip default-jdk g++ gcc
      npm install
      npm run build:shared
      npm run build -w backend
    startCommand: npm run start -w backend